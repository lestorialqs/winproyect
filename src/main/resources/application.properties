spring.application.name=winproyect

# Generar el log de las consultas en formato legible
spring.jpa.properties.hibernate.format_sql=true
PGHOST=localhost
PGPORT=5555
POSTGRES_DB=postgres
PGUSER=postgres
PGPASSWORD=postgres
spring.datasource.url=jdbc:postgresql://${PGHOST}:${PGPORT}/${POSTGRES_DB}
spring.datasource.username=${PGUSER}
spring.datasource.password=${PGPASSWORD}
spring.jpa.hibernate.ddl-auto=create

# docker run --name postgres_container -e POSTGRES_PASSWORD=postgres -p 5555:5432 -d postgres